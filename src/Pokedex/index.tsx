import React from 'react';
import {Container, PokemonContainer, PokemonTitle} from './styles';

const Pokedex: React.FC = () => (
  <Container>
    <PokemonContainer>
      <PokemonTitle>Pikachu</PokemonTitle>
    </PokemonContainer>
  </Container>
);

export default Pokedex;
